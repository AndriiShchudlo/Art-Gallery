import { Component, Input } from '@angular/core';

@Component({
  selector: 'log-field',
  templateUrl: './app.login-field.html',
  styleUrls: ['./app.login-field.css']
})

export class LoginField {
  
}